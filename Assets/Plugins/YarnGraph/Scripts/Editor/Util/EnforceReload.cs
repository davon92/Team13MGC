#if XNODE
#if UNITY_EDITOR
using UnityEngine;

namespace Arawn.YarnGraph
{
	public class EnforceReload : MonoBehaviour
	{
		// Start is called once before the first execution of Update after the MonoBehaviour is created
		void Start()
		{

		}

		// Update is called once per frame
		void Update()
		{

		}
	}
}
#endif
#endif